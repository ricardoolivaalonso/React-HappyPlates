const colors = {
    black: "#111111",
    white: "#F9F9F9",
    gray: "#929292",
    shadow: "#000000",
}
const size = { 
    mobile: '750px',
}
const queries = {
    mobile: `(max-width:${size.mobile})`
}
const theme = {
	// colors,
    size,
    queries
}

export { theme }

