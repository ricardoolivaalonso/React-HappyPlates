import styled from "styled-components"

const CardCategorieElement = styled.p`
    grid-area: cat;
    width: 100%;

    font-size: calc(var(--font-root) * .75);
    font-weight: 300;
    color: var(--gray);
    text-transform: capitalize;
`

export { CardCategorieElement }